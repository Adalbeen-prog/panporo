<%= form_with(url: 'search', method: 'get', class: "ui form", local: true) do %>

<div class="field">
  <div class="ui icon input">
    <input type="text" placeholder="Jméno vyvolávače" name="summoner">
    <i class="inverted circular user alternate link icon"></i>
  </div>
</div>

<% end %>
