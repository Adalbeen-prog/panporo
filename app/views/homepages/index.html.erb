<%= form_with(url: 'search', method: 'get', class: "form-inline", local: true) do %>
  <div class="form-group">
    <div class="ui icon input">
      <label for="summoner">Najít profil</label>
      <input type="text" id="summoner" placeholder="Jméno vyvolávače" name="summoner" class="form-control">
      <button type="submit" class="btn btn-primary">Vyhledat</button>
    </div>
  </div>
<% end %>

<%= form_with(url: 'live', method: 'get', class: "form-inline", local: true) do %>
  <div class="form-group">
    <div class="ui icon input">
      <label for="summoner">Právě probíhající hra</label>
      <input type="text" id="summoner" placeholder="Jméno vyvolávače" name="summoner" class="form-control">
      <button type="submit" class="btn btn-primary">Vyhledat</button>
    </div>
  </div>
  <br>
<% end %>

